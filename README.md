# AISD_Stepawka

Сложность <https://github.com/Stepawka-hub/AISD_Stepawka/blob/main/Lab%20%E2%84%961/Lab%20%E2%84%961/dynamic_array.h#L93> в худшем случае O(1) (константное время),
так как массив - это непрерывная область памяти. Мы обращаемся к элементу массива по индексу, без перебора n-ого количества элементов массива.

Сложность <https://github.com/Stepawka-hub/AISD_Stepawka/blob/main/Lab%20%E2%84%961/Lab%20%E2%84%961/linked_list.h#L156> в худшем случае O(n).
Просто вставка нового узла в алгоритм занимает константное время.
Но для выполнения этого действия вызывается функция получения указателя на узел getnode(int index), сложность которой в худшем случае оценивается в O(n), так
как искомый узел может находиться в конце списка (n).
" Забыл про getnode(), но для функции getdata() учёл, что цикл начинает перебирать элементы с начала или конца списка в зависимости от индекса. 

Сложность <https://github.com/Stepawka-hub/AISD_Stepawka/blob/main/Lab%20%E2%84%961/Lab%20%E2%84%961/TApplication.cpp#L569> в худшем случае O(n), потому что необходимо прогнать каждый токен в очереди (в худшем случае n элементов) в цикле (сравнить и выполнить какую-либо операцию);

